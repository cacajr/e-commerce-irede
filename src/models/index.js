const usersModel = require('./users')
const productsModel = require('./products')
const salesModel = require('./sales')
const usersProductsModel = require('./usersProducts')

module.exports = {
    usersModel,
    productsModel,
    salesModel,
    usersProductsModel
}